# Agent Instructions

Read `docs/progress.md`, `docs/next-steps-plan.md`, and `docs/github-operation-ledger.md` before any GitHub write action.

Rules:

- Use `docs/progress.md` as the milestone state source.
- Select the first TODO milestone.
- Declare operation state before GitHub write actions.
- Use one branch and one PR per milestone.
- Use CI as VERIFY.
