# Product Idea

Build a small task-board web app for a solo developer.

The app should support creating tasks, moving tasks across columns, filtering by status, persisting state, and showing a clean onboarding path. The generated repository is intentionally minimal; it exists to demonstrate the GitHub Loop Runner handoff and long-run growth scaffold.
